/* -*- Mode: C++; tab-width: 2; indent-tabs-mode: nil; c-basic-offset: 2 -*- */
/**
 * \file NetLogListener.cpp
 * class NetLogListener
 *
 * $Id$
 *
 */

#include "NetLogListener.h"

using namespace NetDJ;

NetLogListener::NetLogListener(const QString aHost, const unsigned int aPort)
{
  if (aHost && aPort) {
  }
}
